# name: CI

# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main, develop]

# # Cancel older runs on the same branch when new commits land
# concurrency:
#   group: ci-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   lint-and-test:
#     runs-on: ubuntu-latest

#     # Spin up real services so integration tests can hit a real DB
#     # instead of mocking everything. Remove any you don't need yet.
#     services:
#       mongodb:
#         image: mongo:7
#         ports:
#           - 27017:27017
#       postgres:
#         image: postgres:16
#         env:
#           POSTGRES_USER: test
#           POSTGRES_PASSWORD: test
#           POSTGRES_DB: test
#         ports:
#           - 5432:5432
#         options: >-
#           --health-cmd "pg_isready -U test"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"
#           cache: "pip"

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -e ".[dev]"

#       - name: Lint (ruff)
#         run: ruff check .

#       - name: Format check (ruff)
#         run: ruff format --check .

#       - name: Run tests
#         env:
#           MONGO_URI: mongodb://localhost:27017
#           POSTGRES_URI: postgresql://test:test@localhost:5432/test
#         run: pytest -v --maxfail=1 --disable-warnings

#   docker-build:
#     runs-on: ubuntu-latest
#     needs: lint-and-test
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       # Build only (no push) — this job's job is to prove the image
#       # still builds cleanly, so a broken Dockerfile fails the PR.
#       - name: Build image
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           push: false
#           tags: data-pipeline:ci
#           cache-from: type=gha
#           cache-to: type=gha,mode=max


# # name: CI

# # on:
# #   push:
# #     branches:
# #       - main
# #       - develop
# #   pull_request:
# #     branches:
# #       - main
# #       - develop
# #   workflow_dispatch: {}

# # permissions:
# #   contents: read
# #   pull-requests: write
# #   issues: write
# #   actions: write
# #   deployments: write
# #   contents: write
# #   packages: write
# #   security-events: write
# #   checks: write
# #   id-token: write
# # jobs:
# #   test:
# #     runs-on: ubuntu-latest
# #     steps:
# #       - uses: actions/checkout@v4
# #       - uses: actions/setup-python@v5
# #         with:
# #           python-version: "3.13"
# #       - run: pip install -r requirements.txt
# #       - run: pip install ruff mypy pytest pytest-cov
# #       - run: ruff check .
# #       - run: ruff format --check .
# #       - run: mypy src
# #       - run: pytest