# services:
#   pipeline:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     env_file:
#       - .env
#     volumes:
#       # Code as a bind mount too, so you can edit locally and rerun
#       # without rebuilding the image while developing.
#       - ./src:/app/src
#       - ./configs:/app/configs
#       - ./data:/app/data
#       - ./logs:/app/logs
#       - ./reports:/app/reports
#     depends_on:
#       mongodb:
#         condition: service_healthy
#       postgres:
#         condition: service_healthy

#   mongodb:
#     image: mongo:7
#     ports:
#       - "27017:27017"
#     volumes:
#       - mongo_data:/data/db
#     healthcheck:
#       test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

#   postgres:
#     image: postgres:16
#     environment:
#       POSTGRES_USER: pipeline
#       POSTGRES_PASSWORD: pipeline
#       POSTGRES_DB: pipeline
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U pipeline"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

# volumes:
#   mongo_data:
#   postgres_data:

# # Usage:
# #   docker compose up --build        # run everything
# #   docker compose run --rm pipeline pytest   # run tests inside the container
# #   docker compose down -v           # tear down + wipe DB volumes



# # name: Docker

# # on:
# #   workflow_run:
# #     workflows: ["CI"]
# #     types:
# #       - completed

# # permissions:
# #   contents: read
# #   actions: read   # required for type=gha cache backend

# # jobs:
# #   docker:
# #     if: ${{ github.event.workflow_run.conclusion == 'success' }}
# #     runs-on: ubuntu-latest
# #     steps:
# #       - name: Checkout
# #         uses: actions/checkout@v4

# #       - name: Set up Docker Buildx
# #         uses: docker/setup-buildx-action@v3

# #       - name: Login to Docker Hub
# #         uses: docker/login-action@v3
# #         with:
# #           username: ${{ secrets.DOCKER_USERNAME }}
# #           password: ${{ secrets.DOCKER_PASSWORD }}

# #       - name: Build and push
# #         uses: docker/build-push-action@v6
# #         with:
# #           context: .
# #           file: ./Dockerfile
# #           push: true
# #           tags: |
# #             pannguyen2001/data-pipeline:latest
# #             pannguyen2001/data-pipeline:sha-${{ github.sha }}
# #           cache-from: type=gha
# #           cache-to: type=gha,mode=max,shared=true